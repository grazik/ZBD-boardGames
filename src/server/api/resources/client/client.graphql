type Client {
    CLIENT_ID: String!
    NUMBER_OF_RENTED_GAMES: Int!
    USER: User!
    ACHIEVEMENTS: [Achievement]!
}

extend type Query {
    getClient(id: ID!): Client!
    getClients: [Client]!
}