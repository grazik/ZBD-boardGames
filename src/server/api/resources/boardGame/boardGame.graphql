type BoardGame {
    GAME_ID: Int!
    TITLE: String!
    DESCRIPTION: String
    NUMBER_OF_PLAYERS: String!
    AVAILABILITY: Int!
    BAIL: Float!
    OPINION: Float
    CATEGORY: [String]!
    IMAGE: String
}

extend type Query {
    getGame(id: ID!): BoardGame!
    getGames: [BoardGame]!
    getFilteredGames(categories: String, availability: String, min: String, max: String): [BoardGame]!
    rentGame(clientID: ID!, gameID: ID!): Int!
    returnGame(gameID: ID!): Int!
}