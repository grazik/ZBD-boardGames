type User {
    USER_ID: String!
    NAME: String!
    LAST_NAME: String !
    ADDRESS: Address
    EMPLOYEE: Boolean!
}

extend type Query {
    getUser(username: ID!): User!
    getUsers: [User]!
    validate(id: ID!, pwd: String!): User
}